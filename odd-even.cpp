#include <iostream>
using namespace std;

int main(){
    int n;
    cout << "Enter the number of elements in the array: ";
    cin >> n;
    if (n/2==0)
    {
        /* code */
        cout << "The  even number of elements." << endl;
    } else
    {
        /* code */
        cout << "The odd number of elements." << endl;
    }
    return 0;
}